paths:
  train_dir: "data/train"
  test_dir: "data/test"

training:
  epochs: 10
  batch_size: 32
  learning_rate: 0.0001
  val_split: 0.2
  save_dir: "checkpoints"
  log_dir: "runs/skin_model"

model:
  pretrained: true
